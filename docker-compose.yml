version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: automaweb
      POSTGRES_PASSWORD: automaweb
      POSTGRES_DB: automaweb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
